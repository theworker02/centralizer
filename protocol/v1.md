# Protocol v1 schema index

Canonical documentation: [../PROTOCOL.md](../PROTOCOL.md)

Message types and CIR wire shapes are implemented in:

- `internal/protocol`
- `pkg/cir`

This directory exists so language shims and future SDKs can pin a versioned document without importing Go packages.
