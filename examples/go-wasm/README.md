# Go → WebAssembly example

Place a `.wasm` module in this directory. Centralizer will detect it
with the `wasm` adapter.

Invocation is **planned**, not implemented, in v0.1. Shared-memory and
WASM sandbox execution are documented in `ROADMAP.md`.
