# Go → C example

Centralizer detects this tree as C (`CMakeLists.txt`, `.c` sources).

Invocation is **not implemented** in v0.1. Use:

```bash
centralizer detect .
centralizer explain .
```

Call will return `ErrNotImplemented`.
